# Required - Same values as main project
# Use Accelerate for runtime (same database as main app)
DATABASE_URL="prisma+postgres://accelerate.prisma-data.net/?api_key=ey.."
# Point DIRECT_URL to a real Postgres instance for migrations (same as main app)
DIRECT_URL="postgresql://USER:PASSWORD@HOST:PORT/DB?schema=public"

# NextAuth.js - Different URL and SECRET for backoffice security
NEXTAUTH_BACKOFFICE_URL="http://localhost:3001"
NEXTAUTH_SECRET="backoffice-different-secret-change-in-production"
NEXTAUTH_URL=="http://localhost:3001"

# Email configuration (same as main project)
EMAIL_SERVER_HOST="smtp.example.com"
EMAIL_SERVER_USER="user@example.com"
EMAIL_SERVER_PASSWORD="password"
EMAIL_FROM="SafeTap Admin <no-reply@safetap.app>"

# Base URL for backoffice
PUBLIC_BACKOFFICE_BASE_URL="http://localhost:3001"

# Google OAuth (opcional - mismas credenciales que la app principal)
GOOGLE_CLIENT_ID="tu-google-client-id"
GOOGLE_CLIENT_SECRET="tu-google-client-secret"

# GitHub OAuth (opcional - mismas credenciales que la app principal)
GITHUB_CLIENT_ID="tu-github-client-id"
GITHUB_CLIENT_SECRET="tu-github-client-secret"

# Configuración específica del Admin Dashboard
ADMIN_EMAIL="admin@safetap.com"  # Email del admin principal
APP_NAME="SafeTap Admin Dashboard"
APP_URL="https://admin.safetap.com"

# Para desarrollo
NODE_ENV="development"

# Para producción, asegúrate de cambiar:
# NEXTAUTH_URL="https://admin.tusitio.com"
# NODE_ENV="production"
# NEXTAUTH_SECRET="un-secret-super-seguro-y-diferente-para-produccion"
